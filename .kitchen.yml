---
driver:
  name: vagrant
  network:
  - ["private_network", {ip: "192.168.33.41"}]

provisioner:
  name: chef_zero

platforms:
  - name: debian-7.8

suites:
  - name: agent
    run_list:
      - recipe[t3-zabbix::agent]
    attributes:

  - name: server
    run_list:
      - recipe[t3-zabbix::server]
    attributes:
